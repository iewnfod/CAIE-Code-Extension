{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "npm",
            "command": "npm",
            "args": ["run", "compile", "--loglevel", "silent"],
            "type": "shell",
            "problemMatcher": "$tsc-watch"
        },
        {
            "label": "cpc",
            "command": "$(which cpc)",
            "args": ["${file}"],
            "type": "shell"
        }
    ]
}